port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: false
# Keys are injected via LIVEKIT_KEYS env var, not in this file
turn:
  enabled: false
logging:
  level: info
