export function App() {
  // TODO(S21, S22): replace the temporary shell with authentication screens
  // and the main chat interface once the corresponding stories are implemented.
  return <main data-app-shell="pending-s21" />;
}
